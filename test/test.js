var assert = require('chai').assert;
describe('Sample Unit Test', function() {
  describe('#indexOf()', function () {
    it('Get to work writing unit tests!', function () {
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    });
  });

  
});
